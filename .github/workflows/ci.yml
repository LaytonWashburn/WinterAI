name: WinterAI CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Set up Node.js for client tests
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Install and run Vitest tests in client
      - name: Install client dependencies
        working-directory: ./client
        run: npm install

      - name: Run client Vitest tests
        working-directory: ./client
        run: npm run test -- --run

      # Set up Python for server tests
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # Install server dependencies
      - name: Install server dependencies
        working-directory: ./server
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Run server Pytest tests
      - name: Run server Pytest tests
        working-directory: ./server
        run: pytest --maxfail=1 --disable-warnings -q

      # Set up Docker Buildx for multi-platform builds
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # # Log in to Docker Hub
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # # Build and push client image
      # - name: Build and push client image
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: ./client
      #     push: true
      #     tags: yourdockerhubusername/client:latest

      # # Build and push server image
      # - name: Build and push server image
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: ./server
      #     push: true
      #     tags: yourdockerhubusername/server:latest
